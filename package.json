{
  "name": "habit-bit",
  "private": true,
  "version": "0.0.1",
  "description": "habit-bit",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build && node ionic-config-override.js",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.3.2",
    "@stencil/core": "^2.0.1",
    "@stencil/store": "^1.3.0",
    "workbox-build": "^5.1.4"
  },
  "dependencies": {
    "@types/uuid": "^8.3.0",
    "moment": "^2.29.1",
    "uuid": "^8.3.1"
  }
}
